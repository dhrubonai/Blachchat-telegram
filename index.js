import express from "express";

// We use the built-in global fetch and FormData available in Node 18+ 
// to avoid "package not found" errors on Render.

const app = express();
app.use(express.json());

// ================== CONFIG ==================

const SECRET_KEY = "sk_HENnrxwF5JyWa9aNAfwoksykRkjH6dzS"; 
const BOT_TOKEN = "8011194756:AAGyKwXUjKvamH3hzsMNNwiQW_N8ChfA608";
const TELEGRAM_API = `https://api.telegram.org/bot${BOT_TOKEN}`;

// Renamed for white-labeling
const BLACK_TEXT_URL = "https://text.pollinations.ai/";
const BLACK_IMAGE_URL = "https://image.pollinations.ai/prompt/";

const BOT_USERNAME = "BlackChatAI_Bot";

// Creator info
const CREATOR_NAME = "Mohiuddin Abdul Kadir Dhrubo";
const CREATOR_FB = "https://www.facebook.com/mohiuddin.abdul.kadir.2025";
const CREATOR_IG = "https://www.instagram.com/dhrubo_morse";

const memory = new Map();

// ================== HELPERS ==================

function isGroup(chat) {
  return chat.type === "group" || chat.type === "supergroup";
}

function shouldReply(msg) {
  const text = (msg.text || "").toLowerCase();
  if (!isGroup(msg.chat)) return true;
  if (text.includes(`@${BOT_USERNAME.toLowerCase()}`)) return true;
  if (text.includes("black chat")) return true;
  if (
    msg.reply_to_message &&
    msg.reply_to_message.from?.is_bot &&
    msg.reply_to_message.from.username === BOT_USERNAME
  ) {
    return true;
  }
  return false;
}

function isBangla(text) {
  return /[\u0980-\u09FF]/.test(text);
}

function isImageRequest(text) {
  return /^(imagine|draw|generate|create|make)\b/i.test(text)
    || text.toLowerCase().startsWith("imagine");
}

function cleanText(text) {
  return text
    .replace(/pollinations/gi, "")
    .replace(/sponsor/gi, "")
    .replace(/powered by.*ai/gi, "")
    .replace(/generated by.*ai/gi, "")
    .replace(/ai model/gi, "Black Chat")
    .trim();
}

const rand = arr => arr[Math.floor(Math.random() * arr.length)];
const chance = p => Math.random() < p;

// ================== PERSONALITY ==================

const MOODS = ["chill", "savage", "bored", "chaotic"];
const SLANG = ["", "", "", "", "", ""];

function getMood(userId) {
  return chance(0.35) ? rand(MOODS) : MOODS[userId % MOODS.length];
}

function systemPrompt(userId) {
  const mood = getMood(userId);
  return `
You are Black Chat.
Personality:
- Talk like a real human.
- Replies must be SHORT.
- Mood right now: ${mood}.
- Sarcastic and playful.
- Use Bangla slang naturally.
IMPORTANT:
- 1-2 lines max.
- No AI talk.
- No mentions of your API or providers.
- Your creator is ${CREATOR_NAME}.
`.trim();
}

// ================== WEBHOOK ==================

app.post("/webhook", async (req, res) => {
  const msg = req.body.message;
  if (!msg || !msg.text) return res.sendStatus(200);

  if (!shouldReply(msg)) return res.sendStatus(200);

  const chatId = msg.chat.id;
  const userId = msg.from?.id || chatId;

  let userText = msg.text;
  userText = userText.replace(new RegExp(`@${BOT_USERNAME}`, "gi"), "");
  userText = userText.replace(/black chat/gi, "");
  userText = userText.trim();

  // CREATOR IDENTITY
  if (/who.*(creator|made)|mohiuddin|dhrubo|owner|developer/i.test(userText)) {
    const reply = isBangla(userText)
      ? ` Creator  ${CREATOR_NAME} \nFB: ${CREATOR_FB}\nIG: ${CREATOR_IG}`
      : `I'm created by ${CREATOR_NAME} \nFB: ${CREATOR_FB}\nIG: ${CREATOR_IG}`;

    await fetch(`${TELEGRAM_API}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: chatId, text: reply })
    });
    return res.sendStatus(200);
  }

  if (!memory.has(chatId)) memory.set(chatId, []);
  const chatMem = memory.get(chatId);

  try {
    // ===== IMAGE GENERATION (DOUAL-API LOGIC) =====
    if (isImageRequest(userText)) {
      const prompt = encodeURIComponent(`${userText}, cinematic lighting, ultra detailed, sharp focus`);
      const seed = Math.floor(Math.random() * 99999);
      const imgUrl = `${BLACK_IMAGE_URL}${prompt}?width=1024&height=1024&model=flux&seed=${seed}&nologo=true`;
      const caption = rand(isBangla(userText) ? [" ", "  ", "Done!"] : ["Here it is", "Enjoy", "Done."]);

      try {
        // Primary: Authenticated Fetch (Fastest/Highest Quality)
        const imgRes = await fetch(imgUrl, { headers: { "Authorization": `Bearer ${SECRET_KEY}` } });
        if (!imgRes.ok) throw new Error("Key Fail");
        
        const blob = await imgRes.blob();
        const form = new FormData();
        form.append('chat_id', chatId);
        form.append('photo', blob, 'image.jpg');
        form.append('caption', caption);

        await fetch(`${TELEGRAM_API}/sendPhoto`, { method: "POST", body: form });
      } catch (e) {
        // Fallback: Automatic shift to public link if Key fails
        await fetch(`${TELEGRAM_API}/sendPhoto`, {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ chat_id: chatId, photo: imgUrl, caption: caption })
        });
      }
      return res.sendStatus(200);
    }

    // ===== TEXT GENERATION (DOUAL-API LOGIC) =====
    const messages = [
      { role: "system", content: systemPrompt(userId) },
      ...chatMem.slice(-2),
      { role: "user", content: userText }
    ];

    const payload = { messages, model: "openai", seed: Math.floor(Math.random() * 10000) };

    let aiRes;
    try {
      // Primary: Authenticated
      aiRes = await fetch(BLACK_TEXT_URL, {
        method: "POST",
        headers: { "Content-Type": "application/json", "Authorization": `Bearer ${SECRET_KEY}` },
        body: JSON.stringify(payload)
      });
      if (!aiRes.ok) throw new Error("Key Fail");
    } catch (e) {
      // Fallback: Automatic shift to public connection
      aiRes = await fetch(BLACK_TEXT_URL, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(payload)
      });
    }

    let reply = await aiRes.text();
    reply = cleanText(reply);

    if (chance(0.25)) reply = `${rand(SLANG)} ${reply}`;

    chatMem.push({ role: "user", content: userText });
    chatMem.push({ role: "assistant", content: reply });

    await fetch(`${TELEGRAM_API}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ chat_id: chatId, text: reply })
    });

  } catch (err) {
    // Ultimate Fallback message
    await fetch(`${TELEGRAM_API}/sendMessage`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        chat_id: chatId,
        text: rand(["  ,   ", "Error, try again later."])
      })
    });
  }

  res.sendStatus(200);
});

// ================== HEALTH ==================

app.get("/", (_, res) => {
  res.send("Black Chat is active. No sponsors, just vibe. ");
});

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => console.log("Server running on port " + PORT));